# ⚠️  TEMPLATE ONLY — do not commit real values.
# Generate real secrets with:
#   kubectl create secret generic ipl-auction-secrets \
#     --from-literal=DATABASE_PASSWORD=<val> \
#     --from-literal=OPENROUTER_API_KEY=<val> \
#     --from-literal=JWT_SECRET=<val> \
#     --from-literal=OPERATOR_PASSWORD=<val> \
#     -n ipl-auction --dry-run=client -o yaml | kubeseal > sealed-secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: ipl-auction-secrets
  namespace: ipl-auction
type: Opaque
stringData:
  DATABASE_PASSWORD: "CHANGE_ME"
  OPENROUTER_API_KEY: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME_32_CHARS_MINIMUM"
  OPERATOR_PASSWORD: "CHANGE_ME"
